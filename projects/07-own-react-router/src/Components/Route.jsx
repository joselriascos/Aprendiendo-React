/* Devuelve null porque toda la lógica se crea en el 
compontente Router, este solo se crea para tener un componente
Route para cada ruta de la aplicación. */
export function Route({ path, Component }) {
  return null
}
